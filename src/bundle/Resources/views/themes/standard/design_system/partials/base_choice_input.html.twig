{% set class =
    html_classes(
        'ids-input',
        'ids-input--' ~ type,
        'ids-input--' ~ size,
        {
            'ids-input--disabled': disabled,
            'ids-input--error': error,
            'ids-input--required': required
        },
        class
    )
%}

{{ block('content')}}
